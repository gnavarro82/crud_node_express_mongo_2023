{{#if tasks}}
  <table class="table table-bordered table-hover table-dark">
    <thead>
      <tr>
        <th>N°</th>
        <th>Title</th>
        <th>Description</th>
        <th>Operations</th>
      </tr>
    </thead>
    <tbody>
      {{#each tasks}}
        <tr>
          <td>{{@index}}</td>
          <td><strong>{{title}}</strong></td>
          <td>{{description}}</td>
          <td>
            {{#if done}}
              <a
                class="btn btn-success"
                href="/toggleDone/{{_id}}"
              >Hecho</a>
            {{else}}
              <a class="btn btn-dark" href="/toggleDone/{{_id}}">
                Restaurar
              </a>
            {{/if}}
            <a href="/delete/{{_id}}" class="btn btn-danger">
              Eliminar
            </a>
            <a href="/edit/{{_id}}" class="btn btn-info">
              Editar
            </a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="h-100 d-flex align-items-center justify-content-center">
    <div class="text-center text-white">
      <h1 class="text-light">No Hay Tareas</h1>
      <img src="/icons/empty.svg" class="w-75" />
    </div>

  </div>
{{/if}}




{{!-- <table>
  <thead>
    <tr>
      <th>N</th>
      <th>Titulo</th>
      <th>Descripcion</th>
      <th>Operaciones</th>
    </tr>
  </thead>
  <tbody>
    {{#each tasks}}
      <tr>
        <td>{{@index}}</td>
      <td>{{title}}</td>  
      <td>{{description}}</td>
      <td>
      <!--si la trae esta hecha entonces le quitamos el check, regresa a su estado anterio--->
     {{#if done}}
              <a href="/toggleDone/{{_id}}">Hecho</a>
     {{else}}
              <a  href="/toggleDone/{{_id}}">Restaurar</a>
     {{/if}}
    </td>

      <td><a href="/delete/{{_id}}">Eliminar</a></td>
      <td><a href="/edit/{{_id}}">Editar</a></td>
      </tr>
    {{/each}}
  </tbody>
</table> --}}